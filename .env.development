# Frontend local (Vite) -> CRM backend local.
# The CRM service paths already include /api, so the base URL must be the origin only.
# Production keeps using its own VITE_API_BASE_URL configuration.
VITE_API_BASE_URL=http://localhost:4000
